<h2>Post Comments</h2>

<p>
<%= link_to @post.title, @post.url %>
</P>

<p> <%= pluralize(@post.comments.count, "comment") %> </p>

<ul class="unstyled">
<% @post.comments.each do |comment| %>
	<li>
	<%= comment.user.username %> wrote:
	<%= comment.content %>
	</li>
<% end %>
</ul>
</br>

<% if logged_in? %>
<p>
<h4> Create a New Comment </h4>
  <%= form_for [@post, @post.comments.new] do |f| %>
     <%= f.label :content %>
     <%= f.text_area :content, rows: 5 %>
   </br>
     <%= f.submit "Submit Comment", class: "btn btn-large" %>
  <% end %>
</p>

<% else %>
You must <%= link_to "Register", register_path %> | <%= link_to "Login", login_path %> to add comments.
<% end %>
